console.log("Vishal")
const mobileNum = 987654321    // it wil not change at any cost and assign only 
                            // integer value
let mailId = "kissme@gmail.com"  //if we declare again any this variable 
                                //again thenit will show error

var empName = "Mukesh Ambani"  // it will create same variable name and
                            // will new assigned to new variable 
cityLived = "Delhi"           // declare variable without any datatype
                            // its a bad practice

console.table([mobileNum,mailId,empName,cityLived])
/*
if we declare any variable in JS without
 assigning any value in it then it will show Undefined.
*/