"use strict"; // will treat whole file as newer version of java script
//  alert(3+3) // will not use in node it has different wAY to represent
                // because be are using node js not browser

// Make sure that out code will High Readability 

let name = "Vishal" // it will show string type name
let age = 21        // shows numerical type
let male = true     // shows boolean type
// All these above are types of let to use

// number ->> range is 2 to the pow 53
// bigint -> is used for very large number. eg.->stock market,reddit etc
// string -> may be used in ' ' or " " where " "is preferred for best
/// boolean -> true / false
//null -> standalone value(representation of empty value).
// undefined -> when value is not defined
// symbol -> used to define unique 
// typeof -> is used to shoe type of data
console.log(typeof"Vishal")
console.log(typeof age)
console.log(typeof male)
// INTERVIEW QSN
console.log(typeof null)   //object
console.log(typeof undefined)  // undefined




